# Automatically generated by Coqoon -- do not edit directly!
# Make site-specific changes in the CoqoonMakefile.vars file
# (using CoqoonMakefile.vars.template as a template).

COQC = coqc
COQFLAGS = -noglob
override _COQCMD = \
	mkdir -p "`dirname "$@"`" && $(COQC) $(COQFLAGS) "$<" && mv "$<o" "$@"

include CoqoonMakefile.vars

bin/%.vo: src/%.v
	$(_COQCMD)
override COQFLAGS += -R src ""
override COQFLAGS += -R bin ""
override COQFLAGS += $(DK_ITU_SDG_KOPITIAM_LP_COQ_8_4_FLAGS)
override COQFLAGS += $(CHARGE_FLAGS)
OBJECTS = \
	bin/Created repository.vo

all: $(OBJECTS)
clean:
	rm -f $(OBJECTS)

bin/Created repository.vo: src/Created repository.v